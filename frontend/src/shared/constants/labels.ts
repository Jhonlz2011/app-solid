/**
 * Shared UI label constants — used across modules for consistent display.
 */

/** Status labels for boolean is_active fields */
export const isActiveLabels: Record<string, string> = {
    'true': 'Activo',
    'false': 'Inactivo',
};
